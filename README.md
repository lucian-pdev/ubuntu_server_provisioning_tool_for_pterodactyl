# ubuntu_server_provisioning_tool_for_pterodactyl
A tool that takes a standard ubuntu server LTS iso and bundles in the provisioning for automated OS install and the pterodactyl control panel + wings architecture to host different kinds of servers.
